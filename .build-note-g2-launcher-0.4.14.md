Temporary CI-only build input for G2 Launcher 0.4.14. The workflow recovers the validated 0.4.13 pack input, replaces the undefined minified image-queue sleep symbol that aborts every app launch, syntax-checks the bundle, and packs with @evenrealities/evenhub-cli 0.1.13. This branch and PR must not be merged into the catalog repository.
